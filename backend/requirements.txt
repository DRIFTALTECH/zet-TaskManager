# Requires Python >= 3.10 (CI builds on 3.12; use 3.12 in production).
#
# langchain-* 1.x declare requires-python >=3.10. On an older interpreter pip does
# not error clearly — it silently filters those releases out and reports
# "Could not find a version that satisfies the requirement langchain-openai==1.1.3",
# listing only 0.x. If you see that, the interpreter is too old, not the pin.
# Check with:  python3 --version
python-dotenv==1.1.0
fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
psycopg[binary]==3.2.4
psycopg2-binary>=2.9
boto3==1.35.99
bcrypt==4.2.1
PyJWT==2.10.1
email-validator==2.3.0   # pydantic EmailStr — rejects malformed addresses at the schema boundary
certifi==2026.1.4
cryptography==43.0.3
python-multipart==0.0.17
langchain-openai==1.1.3
langchain-core==1.4.7
groq==0.37.1
pypdf==4.3.1
python-docx==1.2.0
fastmcp==2.13.0.2
# Microsoft Graph (Teams meeting transcripts → MOM); httpx ships with fastmcp
msal==1.36.0
httpx==0.28.1
# Optional: error monitoring (activated only when SENTRY_DSN is set)
sentry-sdk==2.20.0
# Optional: multi-worker realtime fan-out (activated only when REDIS_URL is set)
redis==5.2.1
